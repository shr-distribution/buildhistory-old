#!/bin/sh
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable nokia-n900-cmt-gpio.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start nokia-n900-cmt-gpio.service
fi
