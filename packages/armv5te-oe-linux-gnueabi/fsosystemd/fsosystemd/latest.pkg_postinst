#!/bin/sh
if test "x$D" != "x"; then
	OPT="-r $D"
else
	OPT="-s"
fi
update-rc.d $OPT fsosystemd start 39 S .
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable fsosystemd.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start fsosystemd.service
fi
