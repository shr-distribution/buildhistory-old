#!/bin/sh
if test "x$D" != "x"; then
	OPT="-r $D"
else
	OPT="-s"
fi
update-rc.d $OPT fsodeviced defaults 27
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable fsodeviced.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start fsodeviced.service
fi
