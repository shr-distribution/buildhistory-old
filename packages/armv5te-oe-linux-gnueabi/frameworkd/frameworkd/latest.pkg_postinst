#!/bin/sh
if test "x$D" != "x"; then
	OPT="-r $D"
else
	OPT="-s"
fi
update-rc.d $OPT frameworkd defaults 29
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable frameworkd.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start frameworkd.service
fi
