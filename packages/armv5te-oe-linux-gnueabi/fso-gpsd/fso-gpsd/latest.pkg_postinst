#!/bin/sh
if test "x$D" != "x"; then
	OPT="-r $D"
else
	OPT="-s"
fi
update-rc.d $OPT fso-gpsd-sysv defaults 35
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable fso-gpsd.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start fso-gpsd.service
fi
