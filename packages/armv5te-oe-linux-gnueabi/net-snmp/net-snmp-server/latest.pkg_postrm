	if test "x$D" != "x"; then
		OPT="-r $D "
	else
		OPT=""
		/etc/init.d/snmpd stop
	fi
if [ "$D" != "" ]; then
	update-rc.d -f -r $D ${INITSCRIPT_NAME} remove
else
	update-rc.d ${INITSCRIPT_NAME} remove
fi
