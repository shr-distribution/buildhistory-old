#!/bin/sh
if test "x$D" != "x"; then
	OPT="-r $D"
else
	OPT="-s"
fi
update-rc.d $OPT fsoaudiod defaults 30
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable fsoaudiod.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start fsoaudiod.service
fi
