#!/bin/sh
if test "x$D" != "x"; then
	OPT="-r $D"
else
	OPT="-s"
fi
update-rc.d $OPT fsotdld defaults 27
OPTS=""

if [ -n "$D" ]; then
    OPTS="--root=$D"
fi

systemctl $OPTS enable fsotdld.service

if [ -z "$D" -a enable = "enable" ]; then
    systemctl start fsotdld.service
fi
