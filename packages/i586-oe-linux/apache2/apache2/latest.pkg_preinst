#!/bin/sh
if [ -z "$D" -a -f "/etc/init.d/apache2" ]; then
	/etc/init.d/apache2 stop
fi
if type update-rc.d >/dev/null 2>/dev/null; then
	if [ -n "$D" ]; then
		OPT="-f -r $D"
	else
		OPT="-f"
	fi
	update-rc.d $OPT apache2 remove
fi
