	update-alternatives --install /boot/zImage zImage zImage-3.10.10-yocto-standard 31010 || true
	if [ ! -e "$D/lib/modules/3.10.10-yocto-standard" ]; then
		mkdir -p $D/lib/modules/3.10.10-yocto-standard
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 3.10.10-yocto-standard
	else
		depmod -a 3.10.10-yocto-standard
	fi
